export interface ILocationData {
    id?: string | null,
    host: string,
    title: string,
    description: string,
    startDate: string,
    endDate: string,
    image: string,
    website: string,
    coords: number[],
    email: string,
    number: string
}